package fib

import "testing"

/**
算术运算符:
+ - * /
% 求余
++  自增
--  自减

go没有前置的 ++ , --  如  ++a, --b
*/

/**
比较运算符:
==  !=   >   <  >=   <=

==比较数组，go中:
相同维数，并且个数相同的狐族才可以比较
每个元素相同的数组才相等
*/

func TestCompareArr(t *testing.T) {
	a := [...]int{1, 2, 3, 4}
	//b := [...]int{1, 2, 3, 4, 5}
	c := [...]int{1, 2, 3, 4}
	//t.Log(a == b) 编译错误
	t.Log(a == c)
}

/**
逻辑运算符

位运算符 &    |    ^    <<    >>

按位清零,  1表示将表达式左边对应的位 清0
1 &^ 0   -- 1
1 &^ 1   -- 0
0 &^ 1   -- 0
0 &^ 0   -- 0
*/
